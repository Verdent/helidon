client2:
  share-connection-cache: false
  connection-cache-config:
    connection-limit:
      fixed:
        permits: 100
    connection-per-host-limit:
      fixed:
        permits: 5
    host-limits:
      - host: some-host
        limit:
          fixed:
            permits: 2